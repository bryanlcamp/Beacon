{
  "server": {
    "host": "127.0.0.1",
    "port": 54321,
    "protocol": "tcp",
    "comment": "Binds to loopback for local testing only"
  },
  "exchange": {
    "protocol_mode": "auto",
    "protocols": ["ouch", "pillar", "cme"],
    "auto_detect": true,
    "comment": "Auto-detect client protocol or force specific one"
  },
  "performance": {
    "max_connections": 100,
    "thread_pool_size": 4,
    "socket_buffer_size": 65536
  },
  "application": {
    "name": "Beacon Matching Engine",
    "log_level": "info"
  }
}