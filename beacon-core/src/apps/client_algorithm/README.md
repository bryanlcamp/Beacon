# Client Algorithm Application

Trading algorithm runtime with configurable strategies.

## Features

- Multiple algorithm types (TWAP, VWAP, POV)
- Real-time latency tracking
- Market data integration
- Risk management
- Config hot reloading

## Build

```bash
python3 build.py --release
# or
mkdir build && cd build
cmake ..
make
```
